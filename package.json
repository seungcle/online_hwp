{
  "name": "hwp-online",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@rhwp/editor": "^0.7.15"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.40.0",
    "vite": "^6.0.0",
    "wrangler": "^4.98.0"
  }
}
